# Clock-
A responsive digital clock built using HTML, CSS, and JavaScript that displays the current time in real-time. The clock updates every second using JavaScript and is styled with modern CSS for a clean and minimal UI.
